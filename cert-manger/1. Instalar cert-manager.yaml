#Copiar y pegar el código para instalar el chart de cert-manager
helm install cert-manager cert-manager \
	--repo https://charts.jetstack.io \
	--create-namespace --namespace cert-manager
	--set installCRDs=true